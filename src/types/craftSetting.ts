// 边型
export interface SideInfo {
  id: null | number | string
  name: string
}

// 机型
export interface MachineInfo {
  id: null | number | string
  name: string
}

// 组织法
export interface MethodsInfo {
  id: null | number | string
  name: string
}

// 纱线颜色
export interface YarnColorInfo {
  id: null | number | string
  name: string
  color_code: string
}