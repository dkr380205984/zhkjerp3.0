// 包装辅料
export interface PackMaterialInfo {
  id?: null | number | string
  name: string
  unit: string
  calc_type: string
}

// 装饰辅料
export interface DecorateMaterialInfo {
  id?: null | number | string
  name: string
  unit: string
}